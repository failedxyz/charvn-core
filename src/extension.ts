import { GameNode } from "./language/game";

/**
 * An extension takes a parsed CharVN script and builds the project.
 */
export class Extension {
    public build(game: GameNode): void {
        return;
    }
}
